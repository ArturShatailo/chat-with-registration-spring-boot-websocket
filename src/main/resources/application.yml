# Spring Boot configuration
spring:
  # Database
  datasource:
    driver-class-name: org.postgresql.Driver

    url: jdbc:postgresql://localhost:5432/chat_registration #${DATABASE_LINK}
    username: postgres #${DATABASE_USERNAME}
    password: Postgresql #${DATABASE_PASSWORD}
  # JPA properties
  jpa:
    hibernate:
      ddl-auto: update #create-drop
    show-sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    open-in-view: false
    generate-ddl: true

  # Server configuration
server:
  error:
    include-message: always
  port: 8095  #5000 #set your port

email:
  user: support@powerfulpanda.tech #${EMAIL_USER}
  password: DEW#rf3EFWf4$$##fre3242 #${EMAIL_PASSWORD}